import React from 'react';
import Header from '../Header';

const Newcard = (props) => {
  return (
      <div>
          <Header/>
          This is newcard
      </div>
  )
}

export default Newcard;
