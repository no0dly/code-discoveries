import React from 'react';
import Header from '../Header';

const Homepage = (props) => {
  return (
      <div>
          <Header/>
          This is homepage
      </div>
  )
}

export default Homepage;
