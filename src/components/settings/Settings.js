import React from 'react';
import Header from '../Header';

const Settings = (props) => {
  return (
      <div>
          <Header/>
          This is settings
      </div>
  )
}

export default Settings;
